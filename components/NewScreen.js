import { View, Text } from 'react-native'

export const NewScreen = () => {
    return (
      <View>
        <Text style={{fontSize: 25, textAlign:'center'}}>New Screen</Text>
      </View>
    )
  }